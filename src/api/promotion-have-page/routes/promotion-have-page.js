'use strict';

/**
 * promotion-have-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::promotion-have-page.promotion-have-page');
