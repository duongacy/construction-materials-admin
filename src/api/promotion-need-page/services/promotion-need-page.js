'use strict';

/**
 * promotion-need-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::promotion-need-page.promotion-need-page');
