'use strict';

/**
 * promotion-need-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::promotion-need-page.promotion-need-page');
